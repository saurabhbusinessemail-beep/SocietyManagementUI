export interface IDistrict {
    countryCode: string,
    stateCode: string,
    districtCode: string,
    districtName: string,
    postalCode: string,
    districtType: string,
    latitude: number,
    longitude: number
}